import { StyleSheet } from 'react-native'

const config_styles = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
    },
    text: {
      marginVertical: 16,
    },
});

export default config_styles